spring:
  application:
    name: shop
  app:
    jwtSecret: ${JWT_SECRET}
    jwtCookieName: ${JWT_COOKIE_NAME:springBootEcom}
    jwtExpirationInMs: ${JWT_EXPIRATION_MS:86400000}
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
logging:
  level:
    org.hibernate.orm.connections: WARN
    org.hibernate: INFO
